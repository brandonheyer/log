---
title: Quick-capture CLI
captured: 2026-06-11
tags: [tooling, log-itself]
---

A `log add "..."` command that appends a line to the inbox without opening an
editor. The lower the friction, the more actually lands here instead of
evaporating.

Could grow subcommands later (`log spark`, `log today`) but the inbox append is
the 90% case.
